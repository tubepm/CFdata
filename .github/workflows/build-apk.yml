name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:      # 支持手动一键触发编译

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # 屏蔽 2026 年 Node 废弃警告

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - name: Set up Android NDK Env
      run: |
        # 自动寻找并绑定 GitHub 虚拟机自带的 NDK 路径
        NDK_PATH=$(ls -d $ANDROID_HOME/ndk/* | head -n 1)
        echo "ANDROID_NDK_HOME=$NDK_PATH" >> $GITHUB_ENV
        echo "NDK 路径已自动绑定为: $NDK_PATH"

    - name: Install Gomobile and Tools
      run: |
        go install golang.org/x/mobile/cmd/gomobile@latest
        go install golang.org/x/mobile/cmd/gobind@latest
        echo "PATH=$PATH:$(go env GOPATH)/bin" >> $GITHUB_ENV
        export PATH=$PATH:$(go env GOPATH)/bin
        export ANDROID_NDK_HOME=${{ env.ANDROID_NDK_HOME }}
        gomobile init

    - name: Setup Go Module and Fix Code
      run: |
        # 1. 强制将首字母大写 Package 替换为标准小写 package，防止任何手误隐患
        sed -i 's/^Package main/package main/g' cfdata.go
        
        # 2. 规范化初始化依赖关系，并将模块直接命名为 main
        rm -f go.mod go.sum || true
        go mod init main
        go get github.com/gorilla/websocket
        go mod tidy

    - name: Build Android APK
      run: |
        export PATH=$PATH:$(go env GOPATH)/bin
        export ANDROID_NDK_HOME=${{ env.ANDROID_NDK_HOME }}
        
        # 终极修复：使用 main 模块整体打包，既能找到 main 函数，又能把 index.html 完美嵌入！
        gomobile build -target=android -o cfdata.apk main
        
    - name: Upload Finished APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: cfdata-apk
        path: cfdata.apk
