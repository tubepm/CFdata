name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - name: Set up Android NDK Env
      run: |
        NDK_PATH=$(ls -d $ANDROID_HOME/ndk/* | head -n 1)
        echo "ANDROID_NDK_HOME=$NDK_PATH" >> $GITHUB_ENV
        echo "NDK 路径已设为: $NDK_PATH"

    - name: Install Gomobile and Tools
      run: |
        go install golang.org/x/mobile/cmd/gomobile@latest
        go install golang.org/x/mobile/cmd/gobind@latest
        echo "PATH=$PATH:$(go env GOPATH)/bin" >> $GITHUB_ENV
        export PATH=$PATH:$(go env GOPATH)/bin
        export ANDROID_NDK_HOME=${{ env.ANDROID_NDK_HOME }}
        gomobile init

    - name: Prepare Pure Code Environment
      run: |
        # 1. 清除上几次生成的所有残留文件，确保区内干净
        rm -f go.mod go.sum main.go index.html || true
        
        # 2. 强行将你的 cfdata.go 彻底恢复到你提交时的最原始状态
        git checkout -- cfdata.go || true

        # 3. 将第一行的 package cfdata 变更为 package main，满足单独编译 APK 的要求
        sed -i '1s/^package .*/package main/' cfdata.go

        # 4. 关键修复：直接向 cfdata.go 的文件末尾追加标准的 main 入口和后台拉起函数
        # 这种平铺追加不带任何双引号和复杂转义，100% 不会引起 YAML 飘红
        echo "" >> cfdata.go
        echo "func main() {" >> cfdata.go
        echo "	go StartServer(8080)" >> cfdata.go
        echo "	golang.org/x/mobile/app.Main(func(a golang.org/x/mobile/app.App) {" >> cfdata.go
        echo "		for range a.Events() {}" >> cfdata.go
        echo "	})" >> cfdata.go
        echo "}" >> cfdata.go

    - name: Initialize Modules
      run: |
        # 5. 初始化 Go 模块并拉取项目必须的 websocket 依赖
        go mod init cfdata_app
        go get github.com/gorilla/websocket
        go mod tidy

    - name: Build Android APK
      run: |
        export PATH=$PATH:$(go env GOPATH)/bin
        export ANDROID_NDK_HOME=${{ env.ANDROID_NDK_HOME }}
        
        # 6. 整包编译当前目录，生成最终的 APK 文件
        gomobile build -target=android -androidapi=21 -o cfdata.apk .

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: cfdata-apk
        path: cfdata.apk
